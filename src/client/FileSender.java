package client;

import java.nio.ByteBuffer;

public class FileSender {
//    private final Thread thread;
    /* Читает из буфера, пакует в дейтаграмму */
    public FileSender() {
        new Thread(() -> {
            // ...
        }).start();
    }
}
